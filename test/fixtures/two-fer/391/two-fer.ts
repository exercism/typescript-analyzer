class TwoFer {
  static twoFer(
    first: string = "you",
    second: string = "me"
  ): string {
    return "One for " + first
      + ", one for " + second
      + ".";
  }
}

export default TwoFer
